const initialState = {
    // initial state here
  };
  
  const handlecart = (state = initialState, action) => {
    switch (action.type) {
      // cases for different actions here
      default:
        return state;
    }
  };
  
  export default handlecart;
  